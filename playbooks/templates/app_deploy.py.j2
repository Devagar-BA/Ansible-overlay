connect('{{ weblogic_admin_user }}','{{ weblogic_admin_pass }}','t3://{{ admin_server_listen_address }}:{{ admin_server_listen_port }}')

{% for app in deployments %}
deploy('{{ app.name }}', '{{ app.source }}', targets='{{ app.target }}', stageMode='{{ app.stage_mode }}')
{% endfor %}

disconnect()

